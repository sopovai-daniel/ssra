\ProvidesPackage{lmodern}
% Empty stub: pandoc's default template unconditionally loads lmodern, which is
% absent from some TeX installations. Fonts are set via fontspec (FreeSerif),
% so lmodern is not needed; this stub is picked up via TEXINPUTS in build.sh.
