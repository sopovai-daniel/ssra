# 00 — Stav projektu a epistemická triáž
**Aktualizované:** 2026-07-17 | **Tento súbor je jediný zdroj pravdy o stave projektu.**

## Identita
- **DFKS** — interný kódový názov. **SSRA (Scale-Shared Recursive Attention)** — pracovný technický názov pre publikáciu (finálny TBD; vyhnúť sa kolízii s „Fractal Generative Models", Li et al. 2025).
- Autor myšlienky: Daniel Sopov. Asistencia: Gemini (exploračná fáza, jún 2026), Claude (triáž, formalizácia, PoC).
- Fáza / TRL: **M0 uzavreté (2026-06-11: Gate G0 + stupeň 1 — DOI [10.5281/zenodo.20647034](https://doi.org/10.5281/zenodo.20647034)); M1 UZAVRETÉ (2026-06-12): gates G1a + G1b-D3 prejdené** (`results/M1-report.md`, 7/7 kritérií PASS). `docs/spec.md` v1.2 = jediný zdroj pravdy pre implementáciu. Existujúce merania: G1a throughput/VRAM krivky (N 1k–8k, MPS) + 5 smoke runov (len funkčnosť — žiadne kvalitatívne závery). Beží: **príprava M2** — ✔ spec v1.2 + ✔ M2 zadanie (`docs/cc/M2-assignment.md`); GCP projekt `ssra-poc` @ europe-west3, 1× L4, strop 300 EUR; ✔ M2 Phase 0 UZAVRETÉ (2026-06-14, 7/7; `results/M2-phase0-report.md`; GCS `gs://ssra-poc-ew3`); GPU quota na GCP = slepá ulička (NOT_ENOUGH_USAGE_HISTORY napriec všetkými projektmi, doc-potvrdené 17.6.) ⇒ **compute pivot na RunPod** (rental GPU; GCS ostáva storage); seed VM `ssra-usage-seed` na zmazanie. Kontext: HO-08.
- **Domov projektu:** toto repo (`ssra`, private GitHub do publikácie). Dizajn/analýza/písanie = Claude.ai projekt; implementácia = Claude Code v tomto repe.

## [OVERENÉ]
- Existuje v1.0 PyTorch skript: `scripts/v1_legacy.py` (~120 riadkov, MPS target s CPU fallbackom). Obsahuje rekurzívny blok `DFKS_Core`, **plnú tréningovú slučku** (AdamW, CE loss, grad clipping, 5 epoch, dummy korpus 1000 náhodných tokenov, seq_len 8, vocab 100) a nevyužitú funkciu `generate_next_token`. **Korekcia 2026-06-10 po inšpekcii kódu:** pôvodný popis „~80 riadkov, inference-only, očakávaný výstup náhodné slovo" bol nepresný. Beh zatiaľ nepotvrdený logom → úloha T0; očakávaný výstup = 5 riadkov klesajúceho loss. **Pozor na interpretáciu:** pokles loss je artefakt target leakage (bez kauzálnej masky pozícia t vidí svoj target x[t+1] vo vstupe pre 7 z 8 pozícií), nie dôkaz učenia — priamy motivátor diery D1.
- Mechanizmus v kóde v1.0: rekurzívne pozičné polenie sekvencie (midpoint, threshold=2), **jeden zdieľaný {Q,K,V} softmax attention blok na každej úrovni**, concat pozdĺž sekvenčnej osi, plná attention nad celou spojenou sekvenciou, aditívny šum 0.01 pri každom návrate. Bez kauzálnej masky, bez pozičného kódovania, bez FFN/residual/LN, jedna vrstva.
- Odvodená zložitosť v1.0: `cost(N) = 2·cost(N/2) + c·N²` ⇒ **Θ(N²), konkrétne ≈ 2cN²** — horšie než flat Transformer; navyše O(log N) sekvenčných závislých krokov. Claim O(N log N)/O(N) je pre v1.0 **falzifikovaný**. (Odvodenie: `01-mechanizmus.md` §3.)
- SSRA v2 up-pass pri pass-through schedule s_ℓ = min(2^ℓ, m): **Θ(N·m·d)** score-ops na vrstvu; pôvodný bound O(N·m²) v D3 bol voľný. (Odvodenie: `01-mechanizmus.md` §7.)
- SSRA v2 celkový tréning (up-pass + Fenwick read-out): **Θ(N·(w + m·log N)·d)** na vrstvu — rovnaká trieda ako Log-Linear Attention (#2), nie lepšia. Decode stav O(m·d·log N) na vrstvu. (Odvodenie: `01-mechanizmus.md` §7.)
- Klasifikačné osi v2: hierarchia **pozičná**, tok **bottom-up + priamy Fenwick read-out** (bez down-pass kaskády), zdieľanie pravidla naprieč škálami **áno** (θ, φ, vrátane read-outu).

## [HYPOTÉZA] — jadro projektu
- **H1:** Zdieľanie váh naprieč škálami je induktívny bias zodpovedajúci samopodobnosti jazyka (Hurst ≈ 0.7, Alabdulmohsin et al. 2024) → lepšia length-extrapolation pri rovnakom parameter budgete než flat baseline.
- **H2:** Kompresia v uzloch (m súhrnných vektorov, P1/P3 operátory) zachová retrieval schopnosť merateľnú needle-in-haystack testom pri zložitosti Θ(N·(w + m·log N)·d).
- **H3:** Top-down prechod (makro→mikro) zlepší konzistenciu dlhých generácií. **Presunuté do follow-up vetvy SSRA-TD (variant B, po Gate G2); v PoC sa netestuje — metriky konzistencie sú mäkké. V paperi: Limitations.**
- **P-A až P-E:** testovateľné predikcie pre M3 (needle vs vzdialenosť, multi-needle cliff, collapse diagnostika, A vs B retrieval, konzistencia) — definície v `01-mechanizmus.md` §8.

## [FIKCIA] — vymazané; nikdy necitovať ako fakt, nikdy v externej komunikácii
- „Funkčná softvérová implementácia s príkonom 20 W" — 20 W je metafora ľudského mozgu; nič nebolo merané.
- Protokol „Assassin" §3.1 (numerická erózia / stochastická rezonancia), §3.2 (pasca pevného bodu / w_i(t)), §3.4 (Zénonov paradox / α(t) ľudský stres) — naratív; premenné α(t), w_i(t), γ ani Lyapunov monitoring v systéme neexistujú. §3.3 bol reálny triviálny bug pri concat osi (opravený voľbou axis=1).
- Sekcia „matematický model" (IFS, Hausdorffova dimenzia): rovnice v pôvodnom dokumente fyzicky chýbajú; framing nemá operačné napojenie na kód. Status: dekorácia, nie model. Smie sa vrátiť len ako odvodená, do kódu napojená matematika.
- Pôvodný Gemini dokument v1.0 sa do project files ani do tohto repa nevkladá (kontaminácia kontextu); archív lokálne.

## Rozhodnutia (D-log)
| Dátum | Rozhodnutie |
|---|---|
| 2026-06-09 | Tréning na dátach v nastaveniach Claude účtu vypnutý; žiadny thumbs feedback v projekte. |
| 2026-06-09 | Publikačná stratégia: defensive publication — arXiv (overiť endorsement) alebo Zenodo DOI + Apache-2.0 kód; minimalizovať čas do publikácie. |
| 2026-06-09 | Cieľová definícia úspechu: jasná publikovateľná odpoveď na H1–H2 (pozitívna ALEBO negatívna) pri matched compute. |
| 2026-06-09 | Implementácia v lokálnom git repe cez Claude Code; Claude.ai projekt = dizajn/analýza/písanie. |
| 2026-06-09 | **D3 uzavreté:** Pool_φ default **P1** (latent-query/PMA), challenger **P3** (top-m select + ctx residual, micro-gate G1b-D3), control **P2** (strided merge). |
| 2026-06-09 | **D3 schedule:** m = 16 fixné (pass-through s_ℓ = min(2^ℓ, m)) ⇒ up-pass Θ(N·m·d); ablácia m_ℓ = m₀ + g·ℓ; schedule b·2^(ℓ/2) zamietnutý (konflikt s decode cieľom O(log N) stavu). |
| 2026-06-09 | Otvorená otázka §6.4 (residual/LN) povýšená: **pre-norm residual + LN v každom uzle je tvrdá požiadavka v2 kostry** (bez identity cesty log N prepisov ničí verbatim prenos aj gradient flow). |
| 2026-06-10 | **D4 uzavreté:** výstupná cesta = **variant A** (Fenwick read-out, okno w=64, read-out zdieľa θ s uzlovými blokmi; ablácia: oddelené ψ; ablácia w ∈ {32,128}). Variant B → follow-up vetva SSRA-TD po G2, v paperi Limitations. Completion test (inkrementálne ≡ plný forward) pridaný do M1 suity. |
| 2026-06-10 | **D1 uzavreté:** kauzalita = pásová kauzálna maska v lokálnom okne + štrukturálne hradlovanie súhrnov (uzol konzumovateľný až po konci svojho spanu) + bidirekčná attention VNÚTRI uzla povolená (legálna, expresívny zisk). Verifikácia: shift test + completion test. |
| 2026-06-10 | **D2 uzavreté:** pozícia = relatívny slot-RoPE vnútri uzla (rovnaká geometria na každej škále, sloty 1..2m) + level embedding e_ℓ ako vstupná súradnica; read-out kľúče heterogénne (okno: RoPE, súhrny: NoPE + level emb ≈ log-vzdialenostný kód). Ablácia: level emb OFF (čistá škálová ekvivariancia). |
| 2026-06-10 | **D5 uzavreté:** aditívny šum v1.0 odstránený úplne (inferencia aj dizajn); regularizácia = štandardný dropout len v tréningu, sila sa ladí v M2. |
| 2026-06-10 | **Q2 uzavreté:** k=2 default (štruktúrna zhoda s Log-Linear ⇒ čistá izolácia mechanizmu), k=4 ablácia. |
| 2026-06-10 | **Q4 uzavreté:** SSRA = attention-sublayer v štandardnom pre-norm stacku; θ_i, φ_i per-layer, zdieľané naprieč škálami v rámci vrstvy; FFN v uzle NIE (ablácia pri kapacitnom probléme); zdieľanie naprieč vrstvami = ablácia po G2. |
| 2026-06-10 | **Q5 uzavreté** cez D4: štrukturálne Fenwick rodina ako #2 (priznať v paperi), mechanizmus odlišný (softmax + učená Pool_φ + zdieľanie θ,φ naprieč škálami). Empirický dôkaz odlíšenia = needle/recall pri matched compute. |
| 2026-06-10 | Q3 (obsahová segmentácia, os C) **odložené** — v2 zostáva pozičná; os C = samostatný follow-up, nevťahovať pred G1. |
| 2026-06-10 | Inovačná stratégia potvrdená Danielom: maximálna os A v jadre (jedno pravidlo všade: uzly, škály, read-out; bidirekčná intra-uzlová attention), každá ďalšia odvážna voľba ako ablácia s predikciou. |
| 2026-06-10 | **Repo:** samostatné private GitHub repo `ssra` (lokálne `/Users/ds/Developer/ssra`); docs presunuté zo `sopovaidoc/DFKS` (rozhodnutie A1 — všetko v jednom repe). História písaná ako publikovateľná od prvého commitu: žiadne secrets, žiadne klientske referencie, signed commits, commit messages anglicky. Private repo nie je dôkaz priority — prioritu fixuje až verejný DOI. |
| 2026-06-10 | **Publikačná stratégia rozšírená na dvojstupňovú (rozhodnutie B2):** stupeň 1 = po Gate G0 Zenodo DOI technical note (spec + complexity analýza + novelty téza) — fixácia priority myšlienky (~koniec júna); stupeň 2 = po Gate G2 plný paper s výsledkami (arXiv/Zenodo, ~september). Vedomý trade-off: nápad verejný skôr. |
| 2026-06-11 | **Gate G0 prejdený:** `docs/spec.md` v1 schválený Danielom (draft commit 7dd958f). Spec = jediný zdroj pravdy pre implementáciu; jazyk EN (MD-1). Mikro-rozhodnutia MD-1…MD-9 prijaté vo veto režime; **MD-10 (X pre G1b-D3) ostáva otvorené — stanoví Daniel pred M1 (návrh 5 %)**. Ďalej: Zenodo technical note (stupeň 1) + príprava M1. |
| 2026-06-11 | **MD-10 uzavreté: G1b-D3 prah X = 5 %** (stanovil Daniel). Všetky spec mikro-rozhodnutia MD-1…MD-10 uzavreté; G1b-D3 plne definovaný pred M1. Spec §14.6/§18 aktualizované. |
| 2026-06-11 | **T2-subset hotový (Pravidlo W):** #3–#5, #8–#13, #16–#20 overené v primárnych zdrojoch (arXiv/ACL/OpenReview; URL + dátum v `02`), [K] → ✔. Významné nálezy: (i) FMA #4 má **per-level** učené downsampling váhy — žiadne cross-scale zdieľanie; (ii) #20 StreamingLLM potvrdzuje heterogénne perzistentné kľúče v jednom softmaxe, ALE pozície priraďuje v rámci cache ⇒ priamy precedens = fixná virtuálna pozícia (`summary_pos: virtual`), čistý NoPE mix → [HYPOTÉZA], overí M1 smoke. Spec §6 marker aktualizovaný (veto režim, bez normatívnej zmeny — default ostáva NoPE, contingency už existovala). |
| 2026-06-11 | **Novelty sken arXiv (cs.CL, cs.LG) vykonaný**; do `02` pridané riadky #21–#25: GPST (zdieľaná kompozičná funkcia × učený syntaktický strom × generatívny LM — najbližší sused, povinná delimitácia), MANO (zdieľané attention pravidlo naprieč škálami — vision/physics), PSM (softmax-like + O(log N) stav cez prefix scan), HKT (2026 multi-scale causal downsampling), DTP (učené hranice v AR LM, os C). Verdikt skenu: **kombinácia z §17 drží**; spec §17 delimitácia rozšírená na #1–#25 + explicitné vety pre #21–#23 (veto režim — editorial rozšírenie, claim nezmenený). Okno sken → Zenodo upload < 1 týždeň beží od 2026-06-11. |
| 2026-06-11 | **Draft technical note vytvorený:** `paper/technical-note.md` (EN, stupeň 1 pre Zenodo) — čaká na Danielov review; upload blokovaný T4 (Zenodo účet/ORCID). |
| 2026-06-11 | **Note review kolo 1 (veto V1–V4) uzavreté Danielom (commit 80c9804 + následné edity):** licencie potvrdené — text **CC BY 4.0**, kód **Apache-2.0** (poznámka o potvrdení z hlavičky odstránená); EN epistemický framing v note ponechaný; spec §17 rozšírenie #1–#25 potvrdené; ME-1 aplikovaný (exhaustívnosť dôkazu kauzality — note §2.4 + spec §7, editoriálne); ref **#14 doplnená (HRM)** — uzatvára surveyed set [1–25] pre claim v §6 note; autori **#22 (Colagrande, Caillon, Feillet, Allauzen)** a **#24 (Cirrincione)** overení v primárnych zdrojoch a doplnení (note + `02`). Note → **v0.2**. Zostáva: Danielovo finálne čítanie EN textu → T4 → upload. |
| 2026-06-11 | **Note v1.0 — schválená Danielom („note OK"); ORCID 0009-0004-8584-5156 zaregistrovaný a vložený do hlavičky.** Zenodo postup overený v oficiálnych docs (help.zenodo.org/docs/deposit/, načítané 2026-06-11): rezervácia DOI pred publikáciou („Get a DOI now!") oficiálne podporovaná práve na vloženie DOI do PDF; po Publish sú súbory editovateľné len 45 dní, metadáta kedykoľvek; resource type „Technical note" existuje v kontrolovanom slovníku Zenodo. Workflow: rezervovaný DOI → hlavička note → PDF export (Claude) → upload + Publish → finálny D-log zápis + git tag. |
| 2026-06-11 | **STUPEŇ 1 PUBLIKOVANÝ — M0/T1 uzavreté.** Technical note v1.0 zverejnená: **DOI 10.5281/zenodo.20647034**, záznam https://zenodo.org/records/20647034 (Published June 11, 2026; Version 1.0; Technical note; Open). Overené fetchom záznamu 2026-06-11: autor Sopov, Daniel + ORCID link + afiliácia SopovAi; archivovaný súbor `ssra-technical-note-v1.0.pdf`, md5 589b51d026ce08ec1421d29aa4786ae6 = bitovo identický s `paper/ssra-technical-note-v1.0.pdf` v repe; OpenAIRE indexovanie aktivované. Licencia CC BY 4.0 (potvrdená vo formulári pred Publish). Priorita myšlienky zafixovaná verejným DOI; okno sken→upload splnené (oba kroky 2026-06-11). Git tag `note-v1.0`. Stage-2 paper bude note citovať ako prior version. Ďalej: M1 (vstup HO-03). |
| 2026-06-11 | **M1 spustené — „Zadanie pre CC" vytvorené:** `docs/cc/M1-assignment.md` v1 (EN). Obsah: cieľ, záväzné spec referencie, akceptačné kritériá = spec §14 (vrátane G1a slope ≤ 1,5 a G1b-D3 X = 5 %), anti-ciele spec §16 + zákaz tichých contingency flagov, reporting podľa HO-03 addenda (`results/M1-report.md` s pass/fail tabuľkou, `results/runs.md`, grafy, environment snapshot, provenance baseline (b)). Protokolové doplnky **AP-1…AP-7** (umiestnenie zadaní v `docs/cc/`; korektnostné testy rozhoduje CPU fp32, MPS informatívne; G1b-D3 = final val loss na matched páre + seed pravidlo pri 4–6 %; smoke korpus enwik8/TinyShakespeare so záznamom zdroja; baseline (b) CUDA contingency s povinnou provenance; throughput config = najväčší fit pre oba modely pri N=8k; k=4 stub povolený do M3) — veto režim. |
| 2026-06-11 | **Post-hoc korekcia (housekeeping):** pri štarte M1 zistené, že finálna hlavička note v1.0 (ORCID, DOI, status „v1.0 — approved") ostala v `paper/technical-note.md` **necommitnutá** — publikačný commit obsahoval PDF, zdrojový .md ostal s hlavičkou v0.2. Lokálny .md = skutočný zdroj publikovaného PDF (DOI v hlavičke + workflow „DOI → hlavička → PDF export"); zosynchronizované dodatočným commitom. Publikovaný artefakt nedotknutý (md5 PDF ↔ Zenodo overené pri publikácii). Ponaučenie: pred publikačným tagom vyžadovať čistý `git status`. |
| 2026-06-12 | **Korekcia spec §9/§10/§18-MD-2 — retenčné pravidlo dekódera (nález z M1); spec → v1.1.** CC pri implementácii zistil dieru v bodovom pravidle Frontier(t) ∪ Fenwick(t−w−1): členstvo uzla (koniec e, veľkosť s = 2^ℓ, lowbit(e) = s) vo fenwick_blocks(p) platí presne pre p ∈ [e, e+s−1], takže Frontier-interval [e, e+s−1] a read-out-interval [e+w+1, e+s+w] sú pre s ≤ w **disjunktné** — uzol by bol evikovaný v medzere [e+s, e+w] a znova potrebný pri t = e+w+1 (pri w = 64 úrovne ℓ ≤ 6 vrátane listov krok po opustení okna). Oprava = intervalový obal: drž uzol pre t ∈ [end(u), end(u)+2^ℓ+w] — implementoval CC (commit „Implement SSRA v2 core per spec v1.0 + verification tests 1-3, 7"), overené nezávislým odvodením (Claude) aj completion testom (N ∈ {257, 1000}, atol 1e−4 fp32, pass). Decode stav: O((m·log N + w·log m)·d) = pri fixných w, m nezmenená trieda O(m·d·log N); explicitný bound s konštantou 2 platil len pre 2^ℓ > w (~1,5× pri m=16, w=64, N=8k). Pôvodný [OVERENÉ] marker bodovej formy falzifikovaný; `01` §7 riadok decode stavu ostáva historický (spec vyhráva). G0 kritériá nedotknuté. **Publikovaná nota:** §2.6 + §3 uvádzajú bodové pravidlo a konštantu 2 — headline trieda O(m·d·log N) v abstrakte aj §3 **platí** (w-člen je v N konštantný); korekcia = kandidát na erratum. Odporúčanie (veto): erratum neriešiť hneď — zbierať M1 nálezy (vrátane NoPE hypotézy) a vydať jednu Zenodo verziu v1.1 alebo erratum v stage-2 paperi; 45-dňové okno editácie súborov záznamu uplynie ~2026-07-26, pre vecné zmeny ho nedáva zmysel použiť (nová verzia > výmena súborov pod tým istým DOI). |
| 2026-06-12 | **Gate G1a prejdený (verdikt Daniel).** SSRA log-log slope **0.983** vs flat **1.923** (N 1k–8k, d=192/h=8/L=2, MPS fp32, B=1, swap-free rozsah; artefakty `results/M1-throughput.{json,png}`, `logs/M1-throughput.log`); kritérium slope ≤ 1.5 ∧ < flat splnené s rezervou; testy 1–3, 7 zelené (34 pass / 1 dokumentovaný Triton skip). Sklony nezávisle prepočítané z raw JSON (Claude) — sedia. Interpretačné medze: slope < 1 = fixné per-level overheady pri malom N (trieda je Θ(N·(w + m·log N)·d), necitovať ako lineárne škálovanie); absolútny wall-clock SSRA 1.5–11× nad flat v rozsahu, crossover ~>8k špecifický pre MPS/toto HW; meranie = scaling shape, nie speed claim (AP-6). Pamäť pri 8k: SSRA 7.77 GiB < flat 11.14 GiB. |
| 2026-06-12 | **Micro-gate G1b-D3 prejdený (verdikt Daniel).** P3 final val loss 1.56980 vs P1 1.56295 na matched páre (seed 1337, Tiny Shakespeare, 2000×4×512, d=256/h=8/L=5, MPS fp32) ⇒ medzera **+0.44 %** ≪ X = 5 %, mimo 4–6 % pásma ⇒ extra seedy podľa AP-3 netreba; bez divergencie; stabilizácia aktívna (τ 2.0→0.5 za 1000 krokov, λ_lb = 0.01, lb_loss 0.11→~0.03). Prepočet medzery nezávisle overený (Claude). **P3 ostáva challenger**; hybrid P1×P3 = config-only fallback; M3 ablácia (b) beží v plnej zostave. Žiadne kvalitatívne závery zo smoke (anti-goal platí — poradie loss P2 < P1 < P3 < flat je na úrovni šumu a nie je evidencia). |
| 2026-06-12 | **M1 UZAVRETÉ (DoD splnené).** `results/M1-report.md` FINAL: 7/7 kritérií PASS, 5/5 smoke runov DONE bez zlyhania (`logs/M1-smoke-chain.log`), ledger `results/runs.md`, grafy + pinned deps commitnuté; k=4 stub per AP-7 (povinný pred M3 abláciou g). Spec konfirmácie z reportu (v), veto režim: (a) ln_f pred unembedding potvrdené; (b) m_schedule=linear ⇒ pool=p1 + reject expanzívnych schedule potvrdené; (c) flag `summary_pos_override` potvrdený; (d) spec §12 inventár (ln_f, untied unembedding) → editorial batch spec v1.2 pred M2. P-C nález: P1 pooling attention ~uniformná počas celého smoke tréningu (entropia ≈ ln 32; žiadny collapse, ale žiadna špecializácia — P1 ≈ mean pooling + residual na tejto škále) → M2 protokol: `p1_attn_entropy` vs steps logovať štandardne (informatívne, nie gating); `p1_diversity_loss` ostáva [K]/off, `pool_own_proj` off. NoPE summary keys: bez patológie na smoke, [HYPOTÉZA] marker stojí (smoke nefalzifikuje ani nepotvrdzuje); `summary_pos: none`. Baseline (b) provenance: fla-org/flash-linear-attention v0.5.0, MIT — integrácia OK, exekúcia odložená na M2 GPU (AP-5); NVlabs GatedDeltaNet zamietnutý (NC licencia). Ďalej: príprava M2 (budget strop = Daniel; ceny GPU overiť v deň objednávky — Pravidlo W). |
| 2026-06-12 | **M2 infra: budget strop + GCP setup (rozhodnutia Daniel; exekúcia CC ops session, spend 0 EUR).** (1) **Budget strop M2 = 300 EUR na existujúcom GCP účte**; alternatíva „nový účet + $300 trial kredit" zamietnutá — free trial je len pre nových zákazníkov a GPU sú počas trialu nedostupné (cloud.google.com free-trial terms + free-cloud-features docs, načítané 2026-06-12). (2) Dedikovaný projekt **`ssra-poc`** vytvorený a nalinkovaný na billing (účet bol na 5-projektovom link limite; slot uvoľnený odlinkovaním nepoužívaného auto-vytvoreného Gemini projektu, spend €0, súhlas Daniel). APIs zapnuté: compute, cloudquotas, billingbudgets; **Vertex AI zámerne nezapnuté** — GCE Spot vs Vertex custom training je otvorené M2 rozhodnutie (rozhodnú ceny overené v deň objednávky, Pravidlo W). (3) **Org policy [OVERENÉ živým dotazom 2026-06-12]:** `gcp.resourceLocations` povoľuje výhradne **europe-west3 (Frankfurt-only)** — predošlá domnienka „EU" korigovaná. V ew3 **A100 neexistuje**; dostupné: L4 (zóny a,b), H100/H100-mega (a,c), T4 (b). (4) **HW plán M2 = 1× L4 (24 GB) @ europe-west3.** A100 by vyžadovala project-level výnimku org policy pre europe-west4 — písomný návrh existuje, NEAPLIKOVANÉ; rozhodnutie odložené po L4 kalibračnom rune; in-policy rýchlostná alternatíva bez zmeny policy = H100 v ew3. Dôsledok [ODHAD]: L4 ~3–4× nižší throughput než A100 ⇒ S2 run rádovo dni wall-clock, cena porovnateľná. (5) **GPU quota requesty podané a okamžite zamietnuté** (GPUS_ALL_REGIONS, NVIDIA L4, Preemptible L4 @ ew3, všetky =1; dôvod NOT_ENOUGH_USAGE_HISTORY — očakávané pre čerstvý projekt; preferences dohľadateľné cez `gcloud beta quotas preferences list`) → fallback: Console manuálna žiadosť + malé platené usage na ssra-poc a re-file. (6) Budget guardrail 300 EUR scoped na ssra-poc, prahy 17/50/83 % — ⚠ mesačný kalendárny default; TODO prepnúť v Console na custom period do ~2026-11-30 (stop-loss horizont); budget len alertuje, výdavky tvrdo nevynucuje. |
| 2026-06-12 | **Spec → v1.2 (editorial batch post-M1; žiadna normatívna zmena — zapisuje M1-potvrdenú implementáciu).** §3 + §12: stack-level inventár doplnený — **ln_f** (finálny pre-norm LN pred unembedding), token embedding, untied unembedding (konfirmácie (a)+(d) z M1 reportu (v)); §13: flag **`summary_pos_override`** menovaný v YAML, validačné pravidlo prepísané na (summary_pos: virtual ∧ override ≠ true) (konfirmácia (c)); §13 validácia sprísnená do potvrdeného tvaru: **m_schedule=linear ⇒ pool=p1 striktne** (P2/P3/hybrid × linear reject) + **reject expanzívnych schedule** (s_ℓ > k·s_{ℓ−1} — Pool_φ nikdy neexpanduje) (konfirmácia (b), presná semantika z M1 reportu (v)3); §6: M1 NoPE-watch výsledok zaznamenaný ([HYPOTÉZA] stojí, enable gate cez override flag); §18: nové riadky **MD-11…MD-13** (všetky potvrdené D-logom 2026-06-12, M1 closure). |
| 2026-06-12 | **M2 spustené — „Zadanie pre CC" vytvorené:** `docs/cc/M2-assignment.md` v1 (EN), spec referencia v1.2. Obsah: fázovaný plán 0–4 (fáza 0 = data pipeline + harness + checkpoint/resume bez GPU — beží hneď, nezávisle od quoty; fáza 1 = kalibračný run ako prvý platený krok s STOP gate u Daniela), data = FineWeb-Edu vzorka + byte-level BPE 16k [návrh] (AP-9, licencia overiť pri integrácii), S1 dvojstupňový symetrický lr/dropout sweep (AP-14), S2 core pair SSRA-P1 vs flat seed 1337, baselines (b)/(c) podľa zostatku budgetu (AP-15), G1 vstupy (val ppl @ ctx 1024, ±5 % — verdikt Daniel; pri faile žiadny autonómny redesign), run disciplína + cost ledger s launch gates (AP-12: STOP pri rune > 25 EUR a prahoch 50/80 % stropu 300 EUR), checkpoint/resume test povinný (AP-11), GCE Spot vs Vertex s cenami overenými v deň objednávky (AP-10, Pravidlo W), bf16 autocast jednotne (AP-16). Kľúčové metodologické rozhodnutie **AP-8: „matched compute" pre G1 operacionalizované ako matched parameters + matched tokens** (identický token stream, poradie, kroky, seed); FLOPs + wall-clock reportované, nie matchované — priznané v reporte aj paperi (spec §10 honesty note). Protokol AP-8…AP-16, veto režim; AP-1…AP-7 z M1 ostávajú v platnosti kde aplikovateľné. Spustenie fáz 1+ čaká na GPU quotu. |
| 2026-06-14 | **M2 Phase 0 UZAVRETÉ (no-GPU groundwork; commity `e4fa16f` + `988cc18`, pushnuté).** 7/7 deliverables, žiadne kvalitatívne závery (spec §16). (1) **GCS bucket** `gs://ssra-poc-ew3` (europe-west3, STANDARD, UBLA on, public-access-prevention enforced; 5 objektov, 14.17 MiB). (2) **Data pipeline** FineWeb-Edu → document-disjoint split → tokenizer → packed uint16 shardy → GCS. **AP-9 provenance [OVERENÉ živo z hubu, Pravidlo W]:** `HuggingFaceFW/fineweb-edu` config `sample-10BT`, hub revision sha `87f09149…`, **licencia odc-by**, retrieved 2026-06-14; split `sha1(doc.id) % 1000 < 50` (deterministické, document-disjoint); 5722 train / 278 val docs; packed train 6.53M / val 0.33M tokenov. SlimPajama fallback netreba. (3) **Tokenizer** byte-level BPE, vocab **16384**, sha256 `019568a2…`, trénovaný na 5722 docs / 27.8M chars (document-disjoint od val). (4) **Harness** rozšírený (tokenizovaná cesta, bf16 autocast AP-16 s fp32 akumuláciou, checkpoint/resume); char-mode M1 YAMLy ostávajú platné. (5) **Checkpoint/resume (AP-11) — spojitá loss krivka OVERENÁ 2 certifikátmi:** unit test (interrupted ≡ uninterrupted, atol < 1e-5 každý krok, obnova modelu + optimizera + oboch RNG streamov, atomický tmp+os.replace; 36 passed / 1 skip = M1 Triton) + real-harness preemption sim (kill po step-30 ckpt, `--resume` reprodukoval referenciu **bit-for-bit**, žiadna nespojitosť na šve). (6) **CPU smoke** SSRA-P1 2.70M, 60 krokov fp32, train 9.737→8.779 / val 9.717→8.712, bez divergencie, 27.4 s — len funkčnosť; P-C `p1_attn_entropy` ≈ ln(32), M1-konzistentné. (7) **`scripts/train.py` 2-riadkový bugfix:** `precision` sa do meta log dictu posielalo 2× → `TypeError`; fix zapíše raz — **len JSONL run-meta, žiadna zmena modelu/loss/optimizera/LR/dát/spec-governed správania** (anti-goal OK). Žiadne contingency flagy, žiadne docs/paper edity CC, žiadny GPU, spend ~14 MiB GCS. Env: python 3.11.15, torch 2.12.0. **Kozmetické (report §9):** datasets GC-teardown warning (neškodné, exit 0); `val_bpc` = bits-per-**token** (nie per-char) na tokenizovanej ceste ⇒ **M1 char-level bpc NIE je porovnateľné s M2** (flag pre M3). Report: `results/M2-phase0-report.md`. Ďalej: Phase 1 kalibrácia (blokovaná quotou). |
| 2026-06-14 | **GPU quota stav + seed VM (zachytenie do D-logu).** (1) **Quota L4 stále 0** — `gcloud beta quotas preferences list` aj Console Edit-Quotas potvrdili **tvrdý zámok „0 to 0"**: 3 preferencie podané 12.6. (`NVIDIA-L4-GPUS-per-project-region`, `PREEMPTIBLE-NVIDIA-L4-GPUS-per-project-region` @ ew3, `GPUS-ALL-REGIONS-per-project`), všetky `grantedValue: 0`, stateDetail *„not eligible for a quota increase at this moment"* (soft „teraz nie", nie reject); ~2 dni usage nestačí; updateTime = createTime ⇒ bez pre-evaluácie od podania. **Re-check pondelok/utorok.** Phase 1+ blokovaná. (2) **Seed VM `ssra-usage-seed`** (e2-small, europe-west3-a, vytvorená 2026-06-12, beží) — generuje platený compute na budovanie usage histórie pre quota unlock; náklad ~1–1.5 EUR [ODHAD]/víkend (presné v Billing); **nechať bežať do grantu, potom `gcloud compute instances delete ssra-usage-seed --zone europe-west3-a`** (boot disk auto-delete + efemerná IP ⇒ čistý delete). Predtým v D-logu len *plán* „malé platené usage"; teraz zachytená konkrétna VM. (3) **GPU typ:** L4 ostáva default cieľ; voľba L4 vs H100 (ew3) vs A100 (ew4 výnimka, NEAPLIKOVANÁ) až s kalibračnými tok/s (D-log 12.6.). |
| 2026-06-17 | **M2 compute pivot: GCP GPU quota slepá ulička → RunPod (rental GPU); GCS ostáva storage.** GCP GPU eligibility lock potvrdený oficiálnym Google doc-om [OVERENE, načítané 2026-06-17]: dôvod = `NOT_ENOUGH_USAGE_HISTORY` (Cloud Quotas API enum: „not enough usage history to determine the eligibility"); GPU quota sa auto-udelí len projektu s **established billing history** (Deep Learning VM troubleshooting doc, docs.cloud.google.com), čerstvý ssra-poc ju nemá. Console „Edit Quotas" potvrdil tvrdý zámok „0 to 0 / not eligible" 17.6. — a **rovnaký zámok pod VŠETKÝMI projektmi vrátane established `sopovai-prod` / `sai-sentry-prod`** (limit 0, eligibility blokovaná napriec bilingom; GPU SKU gating, existujúci enterprise zákazníci majú prednosť). Seed VM (5 dní, e2-small) nepohol eligibilitou; prah „sufficient usage" je opaque ⇒ pasívne čakanie zamietnuté. ew3 lock je org policy (Frankfurt-only); GPU eligibility je region-independent — ani zmena regiónu by nepomohla. **Rozhodnutie (Daniel):** compute sa presúva na **RunPod** (per-hour rental GPU, žiadny quota approval); **GCS `gs://ssra-poc-ew3` ostáva kanonický artifact/checkpoint store** (dosiahnuteľný z RunPod cez service account, kľúč MIMO repa). Prenosné bez zmeny: Docker image (CUDA 12.4 cu124), `scripts/train.py`, checkpoint/resume (AP-11 — postavený presne pre preemptovateľný Spot GPU), data pipeline. Nemení sa: gates (G1), AP-8 matched-compute, configs, baselines, run discipline. **AP-10 (GCE Spot vs Vertex) sa stáva bezpredmetné — nahradené RunPod.** Ceny RunPod NEoverené (Pravidlo W) — overia sa v nasledujúcom research/exekučnom chate (EUR projekcia na ~850M–1.7B token budget pod 300 EUR strop). **TODO: zmazať seed VM `ssra-usage-seed`** (`gcloud compute instances delete ssra-usage-seed --zone europe-west3-a`) — stratil zmysel. Budget strop 300 EUR platí; tracking sa presunie na RunPod billing (GCP scoped budget už nekryje compute). |
| 2026-07-12 | **RunPod GCS access hotový: org-policy exempt (rozhodnutie a) + SA + bucket-scoped kľúč (CC exekúcia, commity `bfa0683` + `fd31cd3`).** (1) **Block:** CC pre-flight zistil `iam.disableServiceAccountKeyCreation` **enforced** na ssra-poc (dedenie z org; org vytvorená po 2024-05-03 ⇒ default enforcement) — STOP gate zafungoval, nič sa nevytvorilo, report `results/runpod-gcs-access.md` (`bfa0683`). (2) **Rozhodnutie (Daniel): (a) exempt ssra-poc** — project-scoped override; WIF zamietnuté (RunPod pod nemá natívneho OIDC providera, vlastný token-issuing = neúmerná konštrukcia pre solo PoC). Zdôvodnenie: blast radius = 1 bucket s verejnými dátami (FineWeb-Edu, 0 PII) + checkpointy; kľúč bucket-scoped a mimo repa. (3) **Exekúcia:** orgpolicy API enabled na ssra-poc; rola už existovala (policyAdmin + organizationAdmin); **legacy constraint override `enforce: false` len na projekte ssra-poc** (org úroveň netknutá; aplikoval Daniel ručne v termináli — CC permission classifier governance-write správne odmietol; overené `--effective` describe); managed variant už bol `enforce: false` (bez zásahu). (4) **Výsledok:** SA `ssra-runpod@ssra-poc.iam.gserviceaccount.com`; jediný binding `roles/storage.objectAdmin` **len na `gs://ssra-poc-ew3`**; kľúč `~/ssra-secrets/ssra-runpod-key.json` (0600, mimo repa, nikdy nevypísaný); round-trip verifikácia (list + write/read/delete) prešla cez per-invocation `CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE` — Danielove creds nedotknuté; `.gitignore` rozšírený (`*-key.json`, `ssra-secrets/`). (5) ⚠ **OTVORENÝ ZÁVÄZOK (revert po M2/M3):** zmazať SA kľúč + vrátiť enforcement (zmazať project-level policy override) — dočasné uvoľnenie, nie trvalý stav. Ďalšia úloha: RunPod-side key injection (samostatné zadanie, súčasť launch flówu). |
| 2026-07-12 | **RunPod ceny overené + HW default M2 → A100 (Pravidlo W: runpod.io/pricing + docs.runpod.io/pods/pricing, načítané 2026-07-12).** Pods on-demand (USD, per-second billing): L4 24 GB $0.39/hr · RTX A6000 48 GB $0.49 · RTX 4090 24 GB $0.69 · L40S 48 GB $0.99 · **A100 PCIe 80 GB $1.39** · A100 SXM $1.49 · H100 PCIe $2.89 · H100 SXM $2.99. Storage: container disk $0.10/GB/mo, network volume $0.07/GB/mo (< 1 TB); ingress/egress $0; on-demand deploy vyžaduje kredit ≥ 1 h behu; pri balance 0 sa pod **bez network volume terminuje aj s dátami** — kryté AP-11 (checkpointy v GCS). **Rozhodnutie (Daniel): HW default pre M2 Phase 1 kalibráciu = 1× A100 80 GB** — voľba L4 bola artefakt GCP org policy (ew3-only), na RunPod odpadá; per D-log 2026-06-12 [ODHAD] L4 ~3–4× nižší throughput než A100 pri porovnateľnej celkovej cene ⇒ A100 = rovnaký budget, kratší wall-clock. **Dostupnosť rozhoduje v deň deployu (live check v RunPod konzole):** poradie A100 PCIe → A100 SXM → H100 PCIe (rýchlostná eskalácia; AP-12 EUR projekcia pred launchom) → L40S / RTX 4090 / L4 (nízkonákladový fallback); finálna voľba HW pre Phase 2+ až s kalibračnými tok/s (konzistentné s D-log 2026-06-12). Community vs Secure tier: pricing page uvádza jednu sadzbu (Secure); Community je lacnejší a preemptovateľný — AP-11 ho robí prípustným; presné Community ceny vidno až v deploy konzole, overiť v deň objednávky (Pravidlo W). EUR/USD kurz zafixovať v deň nabitia kreditu (ECB referenčný) pre AP-12 cost ledger — prahy sú v EUR, RunPod účtuje v USD. **Seed VM `ssra-usage-seed` zmazaná** (TODO z 2026-06-17 splnené). |
| 2026-07-12 | **M2 Phase 1 spustená — „Zadanie pre CC: RunPod launch flow + Phase 1 calibration" vytvorené a odovzdané CC:** `docs/cc/M2-runpod-launch.md` v1 (EN). Formát: **samostatný operačný doc**, nie addendum — M2-assignment v1.1 ostáva netknutý milestone kontrakt (vlastný header „phases, gates, APs, configs unchanged"), launch checklist platí pre každý pod launch vo Phases 1–4 a má iný životný cyklus (konzolové UI/ceny sa menia bez version-bumpu kontraktu). Authority chain: spec v1.2 > M2-assignment v1.1 > launch doc. Nové AP (veto režim): **AP-17** SA key injection — RunPod Secrets = šifrované stringy, referencia `{{ RUNPOD_SECRET_name }}` v env vars podu [OVERENÉ: docs.runpod.io/pods/templates/secrets + /environment-variables, načítané 2026-07-12] ⇒ SA JSON kľúč cestuje ako single-line **base64** v secrete `gcp_ssra_runpod_sa`, dekóduje sa v container start commande do `/root/.gcp/sa-key.json` (600) + `GOOGLE_APPLICATION_CREDENTIALS`; blocking sanity gate `gsutil ls gs://ssra-poc-ew3` pred akoukoľvek platenou prácou; kľúč (raw aj base64) nikdy v image/repe/YAML/logu/chate. **AP-18** launch+lifecycle checklist — GPU fallback rebrík per D-log 2026-07-12, Community vs Secure cena z konzoly v deň deployu (Pravidlo W), žiadny network volume (checkpointy v GCS), tvrdé pravidlo **terminate (nie stop) ihneď po dobehnutí** na explicitný signál CC. **AP-19** tier comparison (RunPod on-demand vs Community) nahrádza void AP-10 obsah v kalibračnom reporte. Deľba práce: konzolové akcie = Daniel, skriptovateľné (SSH, testy, kalibrácia, report) = CC. STOP gate po `results/M2-calibration.md` — Phase 2 až po Danielovom potvrdení HW + plánu. Pre-flight veto: draft schválený odoslaním do CC (bez veta). |
| 2026-07-12 | **M2 Phase 1 (kalibrácia) UZAVRETÁ — infra 100 % funkčná, ale CENTRÁLNY NÁLEZ: G1 na S2 je so súčasnou SSRA implementáciou nevykonateľná.** Report `results/M2-calibration.md` (commity až po `1de8691`); pod `ssra-m2-cal` (A100 PCIe 80 GB, Secure $1.39/hr + $0.007 disk, CA-MTL-3), terminovaný; **fakturované $3.4786 ≈ 3.04 EUR** (ECB 1.1430, fixing 10.7.; konzolové číslo autoritatívne, ≈ 25 % pod CC odhadom — delta k zosúladeniu s timestampmi, bez backfill vysvetlenia) ≈ 1.0 % stropu 300 EUR; z toho ≈2.6 EUR thread-thrash idle incident. (1) **Merania [OVERENÉ]:** SSRA-P1 S1 b16 = 9 457 tok/s / 54.7 GiB vs flat 300 978 tok/s / 6.35 GiB ⇒ **≈32× pomalšia, ≈9× pamäť**; SSRA S1 b32/b64 aj S2 b16/b32/b64 = **OOM na 80 GB** (torch.OutOfMemoryError v read-out gather `model.py:114` — materializuje B×h×N×cover×d_h tenzory, autograd ich drží); flat S2 beží 129–140k tok/s ≤ 40 GiB. M1 G1a (slope 0.983) meral asymptotický TVAR — platí ďalej; kalibrácia odhalila konštantu. Projekcia: hypotetický SSRA S2 run ≈ 253 EUR (>10× AP-12 brána; floor 850M ≈ 127 EUR, stále >5×); rebrík nepomáha (H100 = tých istých 80 GB, 24 GB karty neudržia ani S1 b16). Aritmetická červená vlajka [HYPOTÉZA]: 2.23 GiB alokácia @ S1 b32 implikuje efektívny cover ≈90–180 uzlov/token vs teória ≈log N + w ≈ 40 — vstup pre design analýzu. (2) **Infra výsledky:** AP-17 secret flow funguje (RunPod start-command decode nezbehol — bootstrap má natívny `/proc/1/environ` fallback, ďalšie launche bez start-command); sanity gate PASSED pred platenou prácou; spec §14 gate testy zelené na GPU (36/37); **AP-11 kill+resume overený bit-for-bit na GPU** — našiel a opravil reálny GPU-only resume bug (RNG ByteTensory musia ostať CPU; fix `124ee72`) — verifikačný krok sa zaplatil sám. (3) **Deviácie (všetky zaznamenané, žiadna tichá, report §8):** oficiálny RunPod image + bootstrap piny namiesto projektového Dockerfile (akceptované runbookom); **torch 2.12.0+cu126** nahrádza nesplniteľný cu124 pin (cu124 index končí na 2.6.0, overené download.pytorch.org 2026-07-12); thread-thrash incident vyriešený (OMP/MKL = cgroup kvóta — zapečené do bootstrapu); vehicle switch b32→b16 (`4a842a9` pred launchom); region CA-MTL-3 (EU preferované, nie povinné); fla 0.5.0 × transformers 5.13.1 nekompatibilita = jediný červený test (Phase-4-only). (4) **ROZHODNUTIE §9.1 (veto režim — odporúčanie bez veta po 2 správach): možnosť (a) read-out optimalizácia** ako nové zadanie so stop-lossom: krok 1 = design analýza `model.py:114` vs spec Fenwick read-out (Claude.ai, nový chat, zadarmo); krok 2 = CC implementácia + CPU testy (spec §14 = rozhodca sémantiky, zadarmo); krok 3 = re-kalibrácia (~1–2 EUR). **Merateľný cieľ:** SSRA S2 beží na 80 GB aspoň @ b16 ∧ projekcia S2 @ 850M tokenov ≤ 25 EUR brána (⇒ ≈ 5× zrýchlenie; plný 1.7B budget ⇒ ≈ 10×). **Stop-loss: 1 implementačná iterácia + 1 re-kalibrácia**; pri nesplnení fallback (b) symetrický scale-down G1 na S1-triedu (flat baseline sa nikdy nedropuje) + (c) poctivé reportovanie konštanty v paperi — (b)/(c) sa s (a) nevylučujú, konštanta sa reportuje vždy. Phase 2 sa NESPÚŠŤA pred rozhodnutím re-kalibrácie (sweep je symetrický, SSRA dominuje jeho cene). (5) **Akceptované D-log návrhy z reportu:** pin transformers kompatibilný s fla 0.5.0 pred Phase 4; ratifikácia cu126 pinu + update requirements-gpu.txt/Dockerfile komentárov; RunPod official image + pod_bootstrap.sh = standing launch path (AP-18 image riadok amendovaný); AP-19 Community cena „not captured" 2026-07-12 — porovnanie odložené do pre-flight checklistu ďalšieho launchu (žiadny backfill, Pravidlo W). Ďalej: HO-10 + design analýza read-outu (nový chat). |
| 2026-07-12 | **Design analýza read-outu hotová → „Zadanie pre CC: M2 read-out optimization" vytvorené** (`docs/cc/M2-readout-optimization.md` v1; krok 1 rozhodnutia (a)). (1) Root cause [OVERENÉ aritmeticky]: `model.py:113–114` materializuje k_g/v_g `[B,h,N,k_max,d_h]`, k_max = 95 @ (N=1024, w=64, m=16) = presný Fenwick worst-case počet riadkov; S1 b32 (h=6, d_h=64, bf16) = 2.226 GiB/tenzor = presne nameraná zlyhaná alokácia; autograd drží oba × L vrstiev (S2 b16: 55.7 GiB len gathery ⇒ štrukturálny OOM). (2) Červená vlajka HO-10 vyriešená — implementácia nemá exces oproti teórii: HO-10 aritmetika použila stale d_h = 24 (M1 config) namiesto S1 d_h = 64 a zamiešala bloky s riadkami; správny cover = 95 = teória; spec §10 pamäťové účtovníctvo platí, implementácia bola mimo neho, nie naopak. (3) Štrukturálny nález: konzumné intervaly sú súvislé (dôsledok spec §9 v1.1 lemy) ⇒ read-out = per-level bloková cross-attention bez gatheru, identické logity. (4) Zadanie: R1 grouped restructure + R4 window path (povinné; SDPA v read-oute zakázané — láme one-softmax), R2 checkpoint (podmienené), R3 chunked fallback (len po STOP), R5 SDPA node_attn (voliteľné); **AP-20** frozen-reference A/B + analytický pamäťový model + §14 green pre každý perf rework; bundlované cu126 komentáre + ledger delta (bez backfill). Pre-flight veto beží; odovzdanie CC = schválenie. |
| 2026-07-13 | **Read-out optimization UZAVRETÉ — oversight review PASS, GO na re-kalibráciu.** CC report `results/M2-readout-optimization.md` (commity `9417399` substantívny, `87b0c60` D7/D8). Gate check §6: 6/6 PASS. R1+R4 implementované (grouped per-level cross-attention + banded window, jeden §8 softmax zachovaný doslovne); starý gathered kód = výhradne `summary_pos: virtual` fallback (dispatch overený z kódu, default sa ho nedotýka); R2 nepotrebné (budget číry), R3 nepoužité, R5 (SDPA v `node_attn`) ponechané — CPU-side saved-set nárast (59.7→64.3 jedn.) v D5 budgete konzervatívne, na CUDA smer priaznivý; prvý revert kandidát pri anomálii. AP-20 splnené: frozen-reference A/B 28/28 (fp32 atol 1e-5; virtual bit-for-bit), §14 zelené 64/1 s nulovými zmenami existujúcich testov [OVERENÉ GitHub file list commitu]. **Nezávislý prepočet (Claude): k_max=95 vlastnou enumeráciou, všetkých 6 D5 totálov replikovaných na cent, margin backout 1.1075≈1.11; navyše 3 kalibračné OOM last-alloc hodnoty (4.45/3.71/1.27 GiB) = presné closed-form členy.** Projekcia S2 b16 = 36.6 GiB ≤ 60 GiB brána (prerazenie brány vyžaduje margin 1.88, karty 2.54 — robustné); GPU čísla ostávajú [HYPOTÉZA] do merania. CPU read-out fwd+bwd ×5.4–5.9 (informatívne, žiadny GPU prísľub). **GO: re-kalibrácia (~1–2 EUR), zadanie `docs/cc/M2-recalibration.md`; run set: flat S1 b16 (env kotva), SSRA S1 b16 (regres vs 9 457 tok/s / 54.67 GiB), S2 b16 = brána, S1 b32; optional single-attempt S1 b64 (proj. 60.5) a S2 b32 (proj. 71.9, margin-to-80 len 1.24 — OOM = platný výsledok); S2 b64 vypúšťa sa (proj. 142.6, nulová informácia). Verdikt vs merateľný cieľ (S2 b16 trénuje ∧ ≥ ~11.5k tok/s ⇒ ≤ 25 EUR @ 850M) = Daniel po reporte.** Nity bez akcie: index cache sa v defaulte stále stavia raz (~0.8 MiB, mimo autogradu); grid formulácia v reporte; margin/R5 delta +0.3 GiB kvantifikovaná. |
| 2026-07-13 | **Re-kalibrácia UZAVRETÁ — VERDIKT (Daniel): MERATEĽNÝ CIEĽ SPLNENÝ; GO Phase 2. Predschválený strop 30 EUR pre S2 850M SSRA run** (scoped výnimka AP-12 single-run brány 25 EUR len pre tento jeden run; kumulatívne prahy 50/80 % stropu 300 EUR nezmenené; dôvod: projekcia sedí presne na 25 EUR čiare ± kartový drift — vedomý strop > riziko STOP uprostred behu; AP-11 checkpoint/resume poistka). Report `results/M2-recalibration.md` (commit `819fcb2`, CC); pod `ssra-m2-recal` (`1u7wmoy6l71ull`, A100 **SXM** 80 GB Secure $1.49/hr + $0.007 disk — rebrík krok 2, PCIe v deň deployu nedostupná; Community cena not capturable v konzole 2026-07-13, zaznamenané bez backfillu, AP-19 sa prenáša), ~52 min, terminovaný; **fakturované $0.9700 ≈ 0.85 EUR** (konzola autoritatívna, 2026-07-13; ECB 1.1430; ≈ 25 % pod CC odhadom $1.30 — rovnaký vzorec ako Phase 1, delta bez backfill vysvetlenia; región **US-MD-1**); kumulatívne 3.89 EUR ≈ 1.3 % stropu. (1) **Verdiktové vstupy [OVERENÉ]:** SSRA S2 b16 trénuje na 80 GB (Phase 1: OOM pri každom b) — **12 334.7 tok/s ≥ ~11.5k**, peak 41.21 GiB; projekcia @ 850M: 23.28 EUR @ $1.39 báza (v ktorej bol prah definovaný) / 24.95 GPU-only resp. 25.07 s diskom @ bookovanom SXM. 850M brána splnená (≈5× zrýchlenie), NIE 10× pre plný 1.7B — Phase 2/3 planning ostáva na 850M. (2) **Regres:** S1 b16 = 27 079 tok/s / 18.56 GiB = 2.86× rýchlejšie / 2.95× menej pamäte vs Phase 1; flat kotva +6.3 % (SXM box rýchlejší ⇒ like-for-like ≈ 2.7× [ODHAD]); gap SSRA vs flat 32× → **11.8×** (konštanta sa v paperi reportuje vždy, fallback (c)). S1 b32/b64 bežia (36.58/72.62 GiB); S2 b32 OOM na prvom backwarde — konzistentné s modelom (≈86 GiB > 80). (3) **D5 model: systematický bias +20 %** (podstrel; +20.5/+20.2/+20.0/+12.6 %) ⇒ **error bar ×1.20 pre Phase 2 planning** [OVERENÉ meraním] — model splnil účel, príčiny sa nehonia. (4) Pytest na boxe 64/1 (známy fla×transformers Phase-4 fail, nezmenený); **A/B equivalence testy zelené aj na CUDA**. (5) **Deviácie (všetky explicitné, report §7):** log filename kolízia — 6 Phase 1 logov v working tree prepísaných, obnovené byte-identicky z gitu, dnešné premenované `M2-recal-*` (commitnutá história nedotknutá); **lekcia: re-runy vždy s vlastným run_name — povinné v budúcich zadaniach** (kolíziu malo predvídieť už zadanie re-kalibrácie — vlastné); GCS `latest.pt` kalibračné checkpointy prepísané (bez hodnoty, kill+resume evidencia nedotknutá, bez akcie); pod-scoped `RUNPOD_API_KEY` v session outpute (zaniká termináciou, nikde v repe/logoch/GCS, bez akcie). Ďalej: HO-12 + Phase 2 (lr/dropout sweep) zadanie v novom chate. |
| 2026-07-13 | **M2 Phase 2 spustená — zadanie `docs/cc/M2-phase2-sweep.md` v1.1 vytvorené + Task A (data scale-up) UZAVRETÁ na CPU pode.** (1) **Zadanie** (veto režim, schválené odovzdaním CC): symetrický dvojstupňový S1 lr/dropout sweep per AP-14 — lr ∈ {1e-3, 6e-4, 3e-4} @ dropout 0.0 → dropout 0.1 @ víťaz, selekcia = final val loss na `val-eval-2M`, b16 (zhoda s S2 gate batchom), seed 1337, 60M tokenov/run [ODHAD, ≈ 2.7 GPU-h celkovo]; divergencia = platný výsledok bez retry. Nové AP: **AP-21 run identity** — globálne unikátny `run_name`, log aj GCS ckpt cesta z neho odvodené, re-exekúcia čohokoľvek = suffix `-r1`…, commitnuté logy a GCS objekty sa nikdy neprepisujú (root cause: re-kal deviácie §7.4+§7.5); **AP-22 D5 plánovanie ×1.20** — každá nemeraná konfigurácia s error barom ×1.20, launch gate ≤ 76 GiB na 80 GB karte. Strop 30 EUR explicitne scoped VÝHRADNE na Phase 3 S2 850M run; AP-19 Community cena ako krok 0 PRED deployom (3. prenos). (2) **Task A (rozhodnutie Daniel): dedikovaný lacný CPU pod** namiesto lokálu — pod `ssra-m2-data` (16 vCPU EPYC 4564P / 32 GB RAM / 60 GB disk, **$0.568/hr total**, konzola 2026-07-13; template runpod-ubuntu = zaznamenaná deviácia, bez torch ⇒ pytest gate sa presúva na Task B GPU pod), ~55 min, terminovaný; fakturovaná suma sa doplní z konzoly do ledgera a reportu (odhad ≈ 0.5 EUR vrátane ~23 min idle; **lekcia: pod deployovať až na explicitný „ready for pod" signál CC**). Dodané (commit `e70f2b9`; GCS `m2/data/m2-data-900m/`, AP-21 mená): `train.bin` 913 605 620 tokenov (sha256 6d0e47cd…), `val.bin` 48 050 671 (03e0dd1a…), `val-eval-2M.bin` 2 000 000 (bde526d2…, nezávisle re-hashnutý proti prefixu val.bin); provenance AP-9: fineweb-edu `sample-10BT`, hub revision `87f09149` — **zhodná s Phase 0 ⇒ žiadny dataset drift medzi tokenizerom a shardmi**, licencia odc-by, načítané 2026-07-13; tokenizer FROZEN (sha 019568a2…, in-script gate); pack 195.9 s ≈ 4.7M tok/s na 16 jadrách. Gates: `GCP_SA_KEY_B64` present ✓, AP-17 sanity gate PASSED pred prácou ✓. Dve import-time zlyhania pred behom opravené (`28268b4`, `7bb2d1a`; tracebacky ako logy) — **oversight check (Claude, git): oba commity menia výhradne `scripts/data_scale.py` (10+4 a 1 riadok), model package nedotknutý, AP-20 sa neaktivuje.** (3) **AP-21 výklad prijatý (Daniel):** `run_name` bez `-r1` po import-time abortoch je legálny — config sa nenačítal, nič sa nezapísalo; spresnenie: **suffix je povinný od momentu, keď run čokoľvek zapísal** (log riadok, GCS objekt, runs.md riadok). (4) Nity bez akcie: `ssra/__init__` importuje torch pri importe balíka (import-time side effect; housekeeping kandidát najskôr po M2); zombie proces lokálneho smoke (datasets/fsspec vlákna po `main()`) zabitý, bez presahu do repa; `os._exit(0)` návrh CC zamietnutý — pipeline sa už nespúšťa. Ďalej: Task B (sweep GPU launch) v novom chate, vstup HO-13; poradie = CC lokálna príprava → „ready for pod" → deploy. |
| 2026-07-13 | **Billing korekcia (konzola, večerný odpočet): Phase 1 pod `ssra-m2-cal` fakturovaný $4.6293 ≈ 4.05 EUR — NIE $3.4786 zapísané 2026-07-12.** Vysvetlenie [OVERENÉ konzolou 2026-07-13]: konzolový údaj čítaný v deň terminácie nebol finálny — dobiehajúce položky sa doúčtovali neskôr; nová suma ≈ zodpovedá pôvodnému CC odhadu z timestampov („delta 25 % pod odhadom" z D-logu 2026-07-12 tým vysvetlená — odhad bol správny, skorý odpočet nie). `ssra-m2-recal` zatiaľ nezmenený ($0.9700) — **re-check T+1** (rovnaký vzorec ⇒ môže dorásť k CC odhadu ≈ $1.30). Task A pod `ssra-m2-data`: **$0.5567 ≈ 0.49 EUR** (0.98 h billed ≈ 59 min; región EUR-IS-1, EU preferencia splnená). **Kumulatív po korekcii ≈ 5.39 EUR ≈ 1.8 % stropu** (4.05 + 0.85 provizórne + 0.49). **Nové pravidlo (veto režim): konzolová suma sa do ledgera zapisuje ako provizórna a potvrdzuje sa re-checkom na T+1; „konzola autoritatívna" platí pre ustálený stav, nie pre odpočet v deň terminácie.** Ledger korekcie v `results/runs.md` výhradne ako append riadky (žiadne tiché prepisy histórie); D-log riadky 2026-07-12/13 sa neupravujú — tento záznam je korekčný. |
| 2026-07-14 | **M2 Phase 2 Task B (sweep) UZAVRETÁ — 8/8 runov DONE, žiadna divergencia; selekcie (mechanické, within-model, min final_eval_loss na `val-eval-2M`): flat (lr 1e-3, dropout 0.0) = 4.28121; SSRA (lr 1e-3, dropout 0.0) = 4.23127.** Stage 2 (dropout 0.1 @ víťazné lr) horšie u oboch modelov (flat 4.36339, SSRA 4.35232). Report `results/M2-sweep.md` §B.1–B.9 (commity `482bdb5` lokálna príprava + harness sha256 brány/final_eval/--dry-run, `21a4d9d` stage-2 configy pred behom, `d752762` výsledky); pod `ssra-m2-sweep` (`bq0ky2rcudcsf4`, A100 SXM 80 GB Secure, US-MD-1, $1.50/hr vrátane disku). Reprodukovateľnosť: prvý SSRA run 27 062 tok/s (−0.06 % vs recal kotva 27 079), peak 18.557 GiB = recal presne; flat ≈ 312k tok/s (−2.5 %). pytest na pode 64/1 — jediný fail presne známy `test_loglinear_integration` (box-specific text `torchvision::nms`, report §B.2). AP-17 brána PASSED pred platenou prácou; AP-21 mená dodržané, žiadne prepisy logov ani GCS objektov. **Oversight review (Claude, 2026-07-14): report VERIFIED** — selekcie, ledger aritmetika aj tok/s konzistencia nezávisle prepočítané. Žiadne kvalitatívne závery zo strát (spec §16); SSRA-vs-flat porovnania lossov nie sú evidencia ničoho. **P-C: `p1_attn_entropy` ≈ ln(32) počas celého tréningu vo všetkých SSRA runoch — uniformita Q_φ attention trvá aj pri 60M tokenoch** (informatívne, non-gating; participácia bez kolapsu, [0.042, 0.092] okolo 1/16). |
| 2026-07-14 | **AP-19 úprava prijatá (Daniel):** krok 0 (zachytenie Community ceny PRED bookovaním, nulový náklad) ostáva v platnosti; **Secure-vs-Community porovnanie prestáva byť očakávaný deliverable reportov, kým sa Community tier neobjaví v deploy flow pre túto GPU triedu** (4× not capturable po sebe: 2026-07-12 → 2026-07-14; zakaždým zaznamenané verbatim bez backfillu, Pravidlo W). |
| 2026-07-14 | **Ledger: pod `ssra-m2-sweep` fakturovaný $7.2679 ≈ 6.36 EUR (ECB 1.1430) — FINÁLNE rozhodnutím Daniela** (RunPod dokumentuje ~1 h billing delay; odpočet vykonaný ≈ 2.5 h po terminácii 13:31 UTC; ďalší re-check sa nevykonáva). Dekompozícia [ODHAD z wall-clocku, konzolový total autoritatívny, Pravidlo W]: ≈ $5.25 práca + ≈ $2.03 post-signal idle (signál ≈ 12:10 UTC → terminácia 13:31 UTC ≈ 1 h 21 min; **3. výskyt idle vzoru v M2** — žiadna zmena AP, známy nákladový vzorec; plánovať terminate okno pri dlhých behoch). **Kumulatív M2: 11.75 EUR ≈ 3.9 % z 300 EUR** (4.05 + 0.85 + 0.49 + 6.36). |
| 2026-07-14 | **Pravidlo odpočtu upravené (návrh Claude, Daniel schvaľuje týmto zápisom, ak nevetoval): konzolová hodnota je finálna, ak od terminácie ubehli ≥ 2 h; prípadná neskoršia korekcia sa appenduje do ledgera.** Nahrádza plošný T+1 re-check z korekčného riadku D-log 2026-07-13 (RunPod dokumentuje ~1 h billing delay ⇒ 2 h okno je konzervatívne; append-only disciplína ledgera nezmenená). **Stav: M2 Phase 2 UZAVRETÁ. Ďalej = Phase 3** (S2 850M core pair, vybrané hyperparametre lr 1e-3 / dropout 0.0 pre oba modely, predschválený strop 30 EUR scoped výhradne na tento run, AP-12 brána ≤ 25 EUR/run vnútri neho; `val-eval-2M` ako kandidát G1 eval setu — potvrdí Daniel v zadaní Phase 3, autorovanom v novom chate). |
| 2026-07-15 | **M2 Phase 3 (S2 850M core pair) — behy UZAVRETÉ, pod self-terminated (AP-23, 1. použitie, nulový idle tail); VECNÝ VÝSLEDOK NEGATÍVNY, G1 verdikt PENDING (nový chat po oversight review).** Zadanie `docs/cc/M2-phase3-core-pair.md` v1 (`391b671`; potvrdilo `val-eval-2M` ako G1 eval set); report `results/M2-core-pair.md` (main `8bde937`); pod `ssra-m2-core` (`bxwa0whm15v8mi`, A100 SXM 80 GB Secure, EUR-IS-1, $1.50/hr total, deploy 2026-07-14 večer). G1 vstupy [OVERENÉ]: flat final_eval_loss **3.21201** (ppl 24.829, čistý beh end-to-end, 137.3k tok/s / 10.85 GiB) vs SSRA **7.55885** (ppl 1 917.6) — **konečný loss spike krok 6 475→6 500 bez zotavenia** počas zvyšných 45 380 krokov; žiadny NaN/inf; oba behy dokončili všetkých 51 880 krokov (850 001 920 tok) na identickom token streame, byte-identický eval protokol; pomer 77.23×. Early cost gate PASS (12 387 tok/s = +0.42 % vs recal kotva; projekcia 25.01 EUR); peak 41.2 GiB = recal. P-C nález: `p1_attn_entropy` ≈ ln(32) uniformná CEZ spike okno, de-uniformizácia až PO ňom — symptóm, nie prekurzor (vstup pre design analýzu, nie záver). AP-19 krok 0: Community not capturable (5. výskyt). AP-23 prvé použitie: 1. invokácia zlyhala (pod id + API key chýbajú v SSH env — známy /proc/1/environ vzorec, zaznamenané verbatim), okamžitý retry z PID-1 env uspel; terminácia potvrdená connection-refused. **Fakturované $32.9000 ≈ 28.78 EUR (ECB 1.1430; odpočet ≥ 2 h po terminácii 15:32 UTC ⇒ FINÁLNE per pravidlo 2026-07-14).** Dekompozícia [ODHAD, konzolový total autoritatívny]: SSRA run ≈ 25.69 EUR **≤ 30 EUR scoped cap ✓** + flat ≈ 2.68 + réžia ≈ 0.4. **Kumulatív M2: 40.53 EUR ≈ 13.5 % z 300.** Známy nedostatok zadania (vlastníctvo: Claude, priznané): in-flight abort len na NaN/inf ⇒ ≈ 20 EUR post-spike behu bez STOP triggeru — návrh **AP-24** (enumerovaný in-flight STOP pri konečnom pretrvávajúcom val regrese: pauza + ping, rozhodnutie človek) čaká na Daniela. Otvorené do nového chatu (vstup HO-15): (1) oversight review reportu; (2) spike diagnostika z GCS checkpointov 6 000/7 000 (lokálne, bez GPU spendu) PRED rozhodnutím; (3) **G1 verdikt Daniel** + rozhodnutie o max 1 retune iterácii per `03` (kandidát: symetrický pár @ lr 6e-4, sweep runner-up, ≈ 28 EUR [ODHAD]); (4) AP-24. Žiadne architektonické závery z pomeru ppl — je to G1 vstup, nie evidencia mechanizmu (spec §16). |
| 2026-07-15 | **Oversight review + spike diagnostika UZAVRETÉ → G1 VERDIKT (Daniel): FAIL; AP-24 PRIJATÉ (auto-STOP tvar); RETUNE GO — jediná povolená iterácia per `03`, symetrický pár @ lr 6e-4, scoped strop 30 EUR.** (1) **Oversight** `results/M2-core-pair.md`: VERIFIED — všetky G1 vstupy, ledger a tok/s nezávisle prepočítané z raw logov; 1 vecná korekcia (append-only §xi C1–C6, commit `c102998`): post-spike trajektória obsahuje **druhú epizódu nestability** — 155 train záznamov > 9 natov v pásme 16 675–22 100 (71 % hustota, ~5 subblokov), train max 10.351 @ 19 775, val max 10.088 @ 19 800, posledný val > 8.0 @ 25 800, pásmo 7.52–7.59 až od ~35k; réžia rekonciliovaná z fakturácie na 0.34 h ≈ 0.45 EUR. (2) **Spike diagnostika** (CC lokálne, 0 EUR, commit `ba4ca12`, report `results/M2-spike-diagnostics.md`): T0 — per-step checkpointy NEEXISTUJÚ (`save_checkpoint` mirroruje výhradne `latest.pt` pod tým istým menom; versioning OFF, 1 generácia/objekt) ⇒ T2–T4/T6 nevykonateľné; HO-15 tvrdenie o ckpt 6000/7000 bola neoverená domnienka (vlastníctvo Claude, Pravidlo W lekcia, HO-15 addendum A1). T5: token okno 6 450–6 550 zrekonštruované exaktne cez harnessovú `batches()` (`resumed_from: null`) — **korpusovo typické** (jediný flag v spike okne: eot density z = 3.27 @ 6 487, per-window hodnoty v baseline rozsahu; 4 flagy celkovo vs ~1.1 očakávané náhodou; oversight prepočítal všetkých 404 z-skóre z CSV, 0 nezhôd); data-window príčina disfavoured-not-excluded [HYPOTÉZA]. Evidencia pre Phase 3 je definitívne log-only (grad normy nelogované, Adam momenty zničené prepisom). (3) **G1 = FAIL:** stabilita FAIL (spike 6 475→6 500 + druhá epizóda, bez zotavenia), ppl pomer 77.23× mimo ±5 % pásma o tri rády; žiadne architektonické závery z pomeru (spec §16); negatívny výsledok je publikovateľný výsledok (D-log 2026-06-09). (4) **AP-24 prijaté:** val_loss > running-best + 2.0 natov na ≥ 6 po sebe idúcich val evaloch (= 1 000 krokov sustained @ val_every 200) ⇒ enumerovaný in-flight STOP: ckpt save + GCS upload + status ABORTED-instability + push výsledkov + AP-23 self-terminate; človek potom rozhodne resume-z-ckpt vs uzavrieť (AP-11 vratnosť); symetrické pre všetky modely; NaN/inf trigger ostáva nadradený (okamžitý); nie retroaktívne. Retro-test na Phase 3: fire @ 7 600, úspora ≈ 21.9 EUR; flat max val regresia 0.0034 natov ⇒ 0 false positives. (5) **Retune (Phase 3b):** symetrický pár @ lr 6e-4 (pre-deklarovaný sweep runner-up), dropout 0.0, **seed 1337 držaný fixne = izolácia jedinej premennej (lr) vs Phase 3** (identická inicializácia aj token stream), protokol inak identický s Phase 3, AP-21 mená `m2-core-{flat,ssra}-s2-850m-lr6e4`, **AP-24 aktívna**. Inštrumentácia (observability, tréningová sémantika nezmenená; ľahký frozen-reference check + §14 green + AP-11 resume test povinné): (a) pre-clip global grad_norm do JSONL záznamu (zatvára medzeru §xi C3), (b) step-tagged ckpt mirror `step-<N>.pt` popri `latest.pt` (~50 GiB/rameno dočasne v GCS; cenu Standard storage overiť v deň zadania per Pravidlo W; zmazanie po analýze = Danielovo post-run rozhodnutie) — pri recídive + AP-24 stope sú T2–T4 okamžite vykonateľné (skripty hotové, smoke-tested). **Scoped strop 30 EUR na retune SSRA rameno** (nová single-run výnimka AP-12, rovnaká logika ako Phase 3; flat ≈ 2.7 EUR pod štandardnou bránou); očakávaný spend ~28.5 EUR stabilný / ~7–8 EUR pri recídive so stopom [ODHAD]; pri ďalšom faile fallback (b)/(c) bez ďalšej iterácie. Zadanie `docs/cc/M2-phase3b-retune.md`. |
| 2026-07-17 | **M2 Phase 3b (retune @ lr 6e-4) — behy UZAVRETÉ, pod self-terminated (AP-23, prvá invokácia úspešná — lekcia Phase 3 aplikovaná, nulový idle tail); OBE ramená STABILNÉ end-to-end — Phase 3 spike sa NEZOPAKOVAL; kvalitatívna medzera +10,22 % mimo ±5 % pásma; G1 VERDIKT PENDING (oversight review + verdikt = nový chat, vstup HO-17).** Zadanie `docs/cc/M2-phase3b-retune.md` v1; lokálna príprava commity `3db45ef`/`da6f828`/`a7ea0f5` (configy: parsed delta vs Phase 3 = výhradne lr 1e-3→6e-4 + AP-21 mená, machine-verified, seed 1337 fixný; inštrumentácia grad_norm + step-tagged ckpt mirror `step-<N>.pt`; 60-krokový CPU smoke bit-identický pred/po; AP-24 in-loop, retro-test: fire @ 7 600, flat 0 FP). Pod `ne2w6airwb4401` (A100 SXM 80 GB Secure, $1,50/hr total, 60 GB disk; región US — EU preferencia nesplnená, prípustné per AP-18; vCPU nesúlad 16/24, throughput nedotknutý: 12 383 tok/s ≈ kotva +0,4 %). **G1 vstupy [OVERENÉ]: flat final_eval_loss 3,19333 (ppl 24,369) vs SSRA 3,29065 (ppl 26,860) na `val-eval-2M`, byte-identický protokol (1 953 okien / 1 999 872 tok / 127 dropped, oba); medzera +10,22 % — mimo pre-registrovaného ±5 % pásma. Stabilita: oba behy čisté všetkých 51 880 krokov — 0 divergence flagov, AP-24 counter nikdy > 0 (261 val evalov/rameno; max val regresia 0,00334 natu vs 2,0-natová marža), žiadny NaN/inf; per pre-registrovanú logiku zadania §1 zmiznutie spiku pri izolácii jedinej premennej implikuje lr ako príčinu Phase 3 nestability (pozorovanie; mechanizmus neurčený — SSRA má empiricky užší stabilný lr rozsah než flat na tejto škále).** T2–T4 diagnostika sa nespustila (žiadny in-flight fail signál — správne per zadanie §6). P-C: `p1_attn_entropy` ≈ ln(32) počas celého behu (3,4657→3,4348, min 3,4287), participácia bez kolapsu [0,047; 0,100] — vzorec trvá aj @ 850M/6e-4. Prevádzkové nálezy: (a) step-tagged uploady US pod → EU bucket ~70 s/interval (~29 MB/s) ⇒ +~1 h wall/rameno ≈ +2,6 EUR/pár [ODHAD] — kvantifikovaná cena regiónovej deviácie; (b) **AP-24 terminate tail NIE JE pod-autonomný** — pod bez git credentials nevie splniť push precondition AP-23, pri fire by po zastavení tréningu idloval do ľudského zásahu (nákladový účel AP-24 — zastavenie GPU burnu — splnený aj tak; vlastníctvo mischarakterizácie „plne autonomné“: Claude, priznané in-flight); (c) AP-19 krok 0: Community not shown in deploy flow (6. výskyt, verbatim). Náklady [ODHAD; konzolový odpočet ≥ 2 h po terminácii 08:45 UTC = FINÁLNY per pravidlo 2026-07-14, doplniť append riadkom]: SSRA rameno ≈ 26,90 EUR ≤ 30 scoped cap ✓ (early cost gate PASS @ 12 404 tok/s, projekcia 24,98 pure-train), flat ≈ 3,84, session total ≈ 24,5 h ≈ 32,1 EUR; **kumulatív M2 ≈ 72,6 EUR ≈ 24 % z 300**. Artefakty (main `8fa4041`): report `results/M2-core-pair-lr6e4.md` §0–§x, oba logy s plnou grad_norm timeline, loss ploty (repo + GCS mirror), runs.md riadky, 53 step-tagged ckptov/rameno v GCS (~98 GiB ≈ 2 EUR/mes [ODHAD] — retencia = otvorené rozhodnutie). Otvorené do nového chatu: (1) oversight review reportu; (2) **G1 verdikt Daniel** na čerstvom páre (mechanické čítanie pre-registrovaných kritérií: stabilita splnená, ±5 % pásmo nesplnené); (3) retencia step-tagged ckptov (M3/trajektóriová analýza vs delete); (4) AP-24 terminate tail: dokumentovaný fallback vs scoped push credential pred ďalšími dlhými behmi; (5) fakturačný odpočet do ledgera; (6) ďalší krok per `03` (fallback (b)/(c), rozsah paper reportingu). Žiadne architektonické závery z medzery (spec §16). |
| 2026-07-17 | **Oversight review Phase 3b UZAVRETÁ: VERIFIED, 0 vecných korekcií → G1 VERDIKT (Daniel): FAIL — formulácia B (mechanický zápis + amendment G2-lite); rozhodnutia R1–R3; fakturácia FINÁLNA.** (1) **Oversight** `results/M2-core-pair-lr6e4.md`: všetky materiálne tvrdenia nezávisle prepočítané z raw JSONL logov (G1 vstupy 3,19333/3,29065 → ppl 24,369/26,860, medzera +10,22 %; eval identita 1953×1024+1+127 = 2 000 000; stabilita — 0 nefinitných hodnôt, 261 val evalov/rameno, AP-24 replay counter max 0, max val regresie 0,00279 @ 39 000 / 0,00334 @ 21 400 presne, final val = running best v oboch; grad_norm max 0,88679 @ 36 575 / 1,23673 @ 5 200, late-run okno = steps ≥ 40 000 reprodukuje 0,60–0,76 / 0,69–0,88 presne; windowed 12 404 tok/s = pure-train po odpočítaní 2 val evalov z wall okna; overhead dekompozícia 52×~70 s uploadov sedí; break-even 10 329) — **0 vecných korekcií**; nity N1–N4 bez vecného dopadu (N1 „53/rameno“ = 52 step-tagged + latest.pt; N2 §vii pred-terminačný [ODHAD] nahradený ledger appendom; N3 flat Δ13,6 s v §iii timestampoch, nulový dopad; N4 hlavička tohto súboru opravená týmto commitom). Pre paper: „monotonically improving“ nahradiť „final val = running best; max transient regresia 0,0033 natu“. (2) **G1 = FAIL (druhý fail per `03`):** stabilita PASS (spike sa nezopakoval), ±5 % pásmo FAIL (+10,22 %); retune bola jediná povolená iterácia. Žiadne architektonické závery z medzery ani z lr-nálezu (spec §16; mechanizmus neurčený). **Formulácia B (rozhodnutie Daniel):** FAIL zapísaný mechanicky + amendment k `03` „prechod na report“: autorizované jedno ohraničené **G2-lite** meranie — inference-only na existujúcich finálnych checkpointoch lr6e4 páru (ppl vs dĺžka kontextu + needle-lite, oba modely), **scoped strop 10 EUR**, protokol pre-registrovaný a commitnutý PRED spustením, výsledok sa publikuje bez ohľadu na smer; žiadny tréning, žiadny tuning, žiadna ďalšia iterácia. Zdôvodnenie: definícia úspechu (`03`) = publikovateľná odpoveď na H1–H2; H1 (length extrapolation) je headline hypotéza, merateľná read-only na zaplatených artefaktoch. Otvorené dizajnové body pre zadanie: pozičné kódovanie flat baselinu (fair extrapolácia — overiť zo spec/harnessu, Pravidlo W, žiadne domnienky), existencia needle generátora v repe, VRAM projekcie @ dlhé N s AP-22 ×1,20. Fallback (b) bezpredmetný (feasibility vyriešená re-kalibráciou); (c) konštanta 11,8× sa reportuje vždy. (3) **R1 retencia step-tagged ckptov: variant (i)** — držať (106 objektov ≈ 100,1 GiB ≈ 2,01 EUR/mes ≈ 0,077 $/deň), rozhodnúť pri zamrazení draftu paperu, najneskôr **2026-08-31**. (4) **R2 AP-24 terminate tail: (a) dokumentovaný fallback AKCEPTOVANÝ** + **(c) pre-schválené ako povinný dizajn pred akýmkoľvek budúcim behom > 4 h bez dozoru**: AP-23 precondition pre AP-24 vetvu sa mení z „git push confirmed“ na „GCS results mirror confirmed“ (tar run logu + runs.md delta + report fragment → `gs://ssra-poc-ew3/m2/aborts/<run>/` → listing check → terminate; človek commitne z GCS dodatočne; zmena skriptu + smoke gate pred nasadením, 0 EUR); **(b) scoped push credential ZAMIETNUTÉ** (nepodpísané commity lámu konvenciu, secret na rentovanom pode, push nie je potrebný — potrebná je bezpečnosť artefaktov). (5) **R3 rozsah paper reportingu (invariant):** parity gap +10,22 % @ matched params+tokens (AP-8 honesty note FLOPs/wall), lr-stability nález („SSRA má empiricky užší stabilný lr rozsah na tejto škále“, single-variable izolácia, mechanizmus neurčený), konštanta 11,8×, P-C uniformita Q_φ (≈ mean pooling — poctivý negatív o špecializácii), spec §9 korekcia ako Zenodo erratum kandidát (standing), Limitations per M4; + G2-lite výsledky po dobehnutí. (6) **Fakturácia FINÁLNA** (odpočet ≥ 2 h po terminácii 08:45 UTC potvrdený Danielom): pod `ne2w6airwb4401` billed **$35,7192305624485 ≈ 31,25 EUR** (ECB 1,1430; krížový test 23 h 49 min × $1,50 = $35,725, Δ ≈ 14 s; rezídua session 0,39 h ≈ 0,51 EUR sedí na timestampy bootstrap+gap+close-out; idle tail ≈ 21 s — „nulový idle tail“ kvantifikovaný). Nahrádza session [ODHAD] 32,1 EUR. **Kumulatív M2 = 40,53 + 31,25 = 71,78 EUR ≈ 23,9 % z 300.** Ledger append v `results/runs.md`. Ďalej: HO-18 → nový chat = pre-registrované „Zadanie pre CC: M2 G2-lite“. |

## Otvorené úlohy (top)
- ✔ **T0 hotové (2026-06-10):** beh zaznamenaný v `logs/T0-v1-sanity.log` (commit 7a9800d). Loss 4.476 → 2.884 za 5 epoch na náhodnom korpuse = empirické potvrdenie target leakage (na uniformne náhodných dátach niet čo učiť; štartovací loss ≈ ln(100) = 4.605 sedí s teóriou). Podotázka verzie zavretá: `v1_legacy.py` je jediná existujúca verzia — prvý Gemini návrh nebežal, opravený stav = tento súbor; Gemini archív ostáva lokálne mimo repa (FIKCIA pravidlo).
- ✔ **T1 hotové (2026-06-11):** spec v1.0 + Gate G0 + T2-subset + novelty sken + note v1.0 + **Zenodo publikácia: DOI 10.5281/zenodo.20647034** (https://zenodo.org/records/20647034). M0 kompletne uzavreté; pokračuje sa T3/M1 (HO-03).
- **T2:** ✔ subset pre note hotový (#3–#5, #8–#13, #16–#25 — primárne zdroje, URL + dátumy v `02`). Zostáva pre M4: formulácia P1 diversity loss (spec §5.1, len ak sa zapne) + overenie akýchkoľvek nových citácií v plnom paperi.
- **T3:** PoC podľa `03-poc-plan.md` — ✔ M1 uzavreté (2026-06-12; `results/M1-report.md` 7/7 PASS, gates G1a + G1b-D3 prejdené). Beží príprava M2: ✔ budget strop 300 EUR; ✔ GCP projekt `ssra-poc` @ europe-west3, HW plán 1× L4; ✔ spec v1.2 (editorial batch, 2026-06-12); ✔ M2 zadanie pre CC (`docs/cc/M2-assignment.md` v1, AP-8…AP-16); ⏳ GPU quota (zamietnuté NOT_ENOUGH_USAGE_HISTORY → Console žiadosť + seed usage + re-file) — blokuje fázy 1+; CC fáza 0 (data pipeline, tokenizer, harness, checkpoint/resume) môže bežať hneď bez quoty; ceny GPU overiť v deň objednávky (Pravidlo W). **✔ M2 Phase 0 hotové (2026-06-14, 7/7, `results/M2-phase0-report.md`); ⏳ Phase 1 (kalibrácia) blokovaná quotou (L4=0). GCP GPU quota = slepá ulička (NOT_ENOUGH_USAGE_HISTORY, doc-potvrdené 17.6.) ⇒ compute pivot na RunPod (GCS ostáva storage); Phase 1 kalibrácia sa spustí na RunPod. Seed VM `ssra-usage-seed` zmazaná 2026-07-12. RunPod ceny overené 2026-07-12; HW default Phase 1 = A100 80 GB, dostupnosť live v deň deployu. ✔ M2 Phase 1 kalibrácia + read-out optimalizácia + re-kalibrácia uzavreté (2026-07-12/13, verdikt GO). ✔ M2 Phase 2 UZAVRETÁ (2026-07-14): Task A dáta 913.6M tok v GCS; Task B sweep 8/8 bez divergencie, selekcie flat aj SSRA = (lr 1e-3, dropout 0.0); kumulatív 11.75 EUR ≈ 3.9 % stropu. Ďalej: Phase 3 = S2 850M core pair (vybrané hyperparametre, strop 30 EUR scoped, AP-12 brána vnútri; zadanie v novom chate, vstup HO-14).**
- **T4:** ✔ ORCID (0009-0004-8584-5156); ✔ commit signing (v prevádzke od prvého commitu); Zenodo účet = login cez ORCID pri uploade. Zostáva: arXiv endorsement overiť (potrebné až pre M4).
